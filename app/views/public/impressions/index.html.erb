<div class="container">
<h2>投稿一覧</h2>
<% @impressions.each do |impression| %>
<div><%= link_to impression_path(impression.id) do %>
<%= impression.title %>
<%= impression.created_at.strftime('%Y/%m/%d') %>
<% end %></div>
<% end %>
</div>